# User data

Types for persisted client-side state used with Svelte context (for example saved cultures on culture-related routes).

Import the default `UserData` type from `$lib/user_data` and wire it through `app.d.ts` / `user_context_provider` as needed.
