/** @deprecated Use ruleset-qualified mechanics from `$lib/rulesets` for new code. */
export {
  convertPowerToDice,
  convertToDnD5e,
  convertToDnDAbilityScore,
  convertToDnDArmorClass,
  convertToDnDModifier,
} from '$lib/rulesets/ironarachne';
