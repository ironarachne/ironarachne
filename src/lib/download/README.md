# Download

Browser-only helper: creates a temporary `<a download>` and clicks it to save a file from a URL (used e.g. for heraldry exports).

Only import from client code (routes/components that run in the DOM).
