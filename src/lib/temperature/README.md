# Temperature

Celsius ↔ Fahrenheit (and Kelvin) helpers and qualitative labels for the environment page, delegating to `$lib/measurements` for conversions.

## Usage

```ts
import * as Temperature from '$lib/temperature';
```
