export default interface SizeGeneratorConfig {
  minHeight: number;
  maxHeight: number;
  minWeight: number;
  maxWeight: number;
  minLength: number;
  maxLength: number;
  minMass: number;
  maxMass: number;
}
