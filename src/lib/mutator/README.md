# Mutator

Tagged mutators and `applyMutators` for procedurally adjusting encounter targets, deities, domains, and similar structures.

`Mutator<T>` is a named, tagged object with a `mutate` function; they chain with a deterministic RNG from the seed.
