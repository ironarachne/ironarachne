/**
 * Single charge on a filled disc (mon-inspired, culture-neutral). High-contrast
 * ground vs charge from the display palette—not heraldic tinctures.
 */
export type DiscEmblem = {
  chargeName: string;
  groundHex: string;
  chargeHex: string;
};
