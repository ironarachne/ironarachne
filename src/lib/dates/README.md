# Dates

Month names/abbreviations and `getNiceDate` for changelog and home page copy (HTML includes ordinal `<sup>` markup).

## Usage

```ts
import { getMonthAbbr, getNiceDate } from '$lib/dates';
```
