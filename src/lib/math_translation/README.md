# Math translation

Small numeric helpers: `clamp` and `linearMap` for remapping values (climate, civilizations, astronomical bodies, etc.).

## Usage

```ts
import { clamp, linearMap } from '$lib/math_translation';
```
