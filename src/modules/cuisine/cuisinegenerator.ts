'use strict';

import CuisineGeneratorConfig from "./cuisinegeneratorconfig";

export default class CuisineGenerator {
  config: CuisineGeneratorConfig;

  constructor(config: CuisineGeneratorConfig) {
    this.config = config;
  }

  generate() {

  }
}
